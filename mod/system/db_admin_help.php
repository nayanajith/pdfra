<h3>Create/Recreate tables and upgrade from previous version</h3>
<p>Please select the action you want to do for each table and press Create/Re-Create to execute the request</p>
<h4>Create/Recreate</h4>
<ul>
<li>CREATE
<li>RECREATE
Rename(backup) the existing table with suffixing the current date if it contains any data and create a new empty table.
</ul>
<h4>Upgrade/Migrate</h4>
If the version of the database is less than the system schema you should upgrade the database to the new version 
